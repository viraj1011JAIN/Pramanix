# SPDX-License-Identifier: AGPL-3.0-only
# Pramanix SLSA Level 3 Release Pipeline â€” v0.6
#
# Trigger: completion of "Publish to PyPI" (publish.yml) on a release/RC tag.
#
# #322 fix: this workflow used to trigger independently on the SAME tag push
# as publish.yml, racing it to call pypa/gh-action-pypi-publish. Whichever
# workflow finished first "won" the PyPI upload; if publish.yml won, the
# release had no SBOM and no Sigstore signatures despite the "SLSA Level 3"
# claim. publish.yml is also the workflow PyPI's trusted-publisher config is
# registered against (owner/repo/workflow=publish.yml/environment=pypi) â€”
# repointing that registration isn't something a workflow file can do, so
# publish.yml remains the sole PyPI publisher. This workflow now runs
# strictly AFTER it succeeds, signs the artifacts it built, and never
# touches PyPI itself â€” no more race, no more divergent SLSA evidence.
#
# Steps (in order):
#   1. Version consistency check  â€” pyproject.toml == tag == __version__
#   2. Build                      â€” poetry build + twine check
#   3. SBOM generation            â€” CycloneDX JSON
#   4. Sigstore artifact signing  â€” .sigstore.json per artifact
#   5. GitHub Release             â€” CHANGELOG section as body, all artifacts attached
#   6. Post-release smoke test    â€” clean venv, pip install, basic import
#
# Permissions:
#   id-token:write     â€” GitHub OIDC for Sigstore keyless signing (NOT PyPI â€” publish.yml owns that)
#   contents:write     â€” create GitHub Release
#   attestations:write â€” Sigstore attestation upload

name: Release (SLSA Level 3)

on:
  workflow_run:
    workflows: ["Publish to PyPI"]
    types: [completed]

permissions:
  id-token: write
  contents: write
  attestations: write

env:
  PYTHONDONTWRITEBYTECODE: "1"
  PYTHONUNBUFFERED: "1"
  FORCE_COLOR: "1"

jobs:
  release:
    name: "Release (post-publish signing)"
    runs-on: ubuntu-latest
    # Only proceed if publish.yml actually succeeded in publishing to PyPI.
    # workflow_run fires on failure/cancellation too â€” skip those.
    if: github.event.workflow_run.conclusion == 'success'

    steps:
      # â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
      # Checkout the EXACT commit publish.yml ran against (not main HEAD) â€”
      # full history so CHANGELOG section extraction works.
      # â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683  # v4.2.2
        with:
          ref: ${{ github.event.workflow_run.head_sha }}
          fetch-depth: 0

      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065  # v5.6.0
        with:
          python-version: "3.13"

      # â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
      # STEP 1 â€” Version Consistency Check
      # Assert pyproject.toml == tag == __version__ == CHANGELOG entry
      # The tag is derived from the checked-out commit (workflow_run does not
      # expose github.ref_name for the upstream tag push).
      # â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
      - name: "Step 0/6 â€” Resolve release tag from publish.yml's commit"
        run: |
          GIT_TAG=$(git describe --tags --exact-match "${{ github.event.workflow_run.head_sha }}")
          echo "Resolved tag: $GIT_TAG"
          echo "GIT_TAG=$GIT_TAG" >> "$GITHUB_ENV"

      - name: "Step 1/6 â€” Version consistency check"
        run: |
          GIT_TAG="${{ env.GIT_TAG }}"
          VERSION="${GIT_TAG#v}"
          echo "Verifying version: $VERSION (tag: $GIT_TAG)"

          # 1a. pyproject.toml
          PYPROJECT_VERSION=$(python - <<'PYEOF'
          import re, sys
          content = open("pyproject.toml").read()
          m = re.search(r'^version\s*=\s*"([^"]+)"', content, re.MULTILINE)
          if not m:
              print("ERROR: could not parse version from pyproject.toml", file=sys.stderr)
              sys.exit(1)
          print(m.group(1))
          PYEOF
          )
          echo "pyproject.toml version: $PYPROJECT_VERSION"
          if [[ "$PYPROJECT_VERSION" != "$VERSION" ]]; then
            echo "::error::Version mismatch: pyproject.toml=$PYPROJECT_VERSION, tag=$VERSION"
            echo "Fix: update [tool.poetry] version in pyproject.toml before tagging."
            exit 1
          fi

          # 1b. src/pramanix/__init__.py __version__
          INIT_VERSION=$(python -c "
          import re, sys
          content = open('src/pramanix/__init__.py').read()
          m = re.search(r'__version__\s*=\s*[\"\']([\d.]+(?:-rc\.\d+)?)[\"\'\"']', content)
          if not m:
              print('ERROR: __version__ not found', file=sys.stderr)
              sys.exit(1)
          print(m.group(1))
          ")
          echo "__init__.py __version__: $INIT_VERSION"
          if [[ "$INIT_VERSION" != "$VERSION" ]]; then
            echo "::error::Version mismatch: __init__.py=$INIT_VERSION, tag=$VERSION"
            echo "Fix: update __version__ in src/pramanix/__init__.py before tagging."
            exit 1
          fi

          # 1c. CHANGELOG.md contains section for this version
          if ! grep -qF "## [${VERSION}]" CHANGELOG.md 2>/dev/null && \
             ! grep -qF "## ${VERSION}" CHANGELOG.md 2>/dev/null; then
            echo "::error::CHANGELOG.md does not contain a section for version $VERSION"
            echo "Fix: add '## [$VERSION] - $(date +%Y-%m-%d)' section to CHANGELOG.md"
            exit 1
          fi

          echo "âœ“ All version checks passed: $VERSION"
          echo "RELEASE_VERSION=$VERSION" >> "$GITHUB_ENV"
          echo "GIT_TAG=$GIT_TAG" >> "$GITHUB_ENV"
          # Detect RC tags (used only for the GitHub Release "prerelease" flag â€”
          # publish.yml publishes both release and RC tags to production PyPI,
          # there is no Test PyPI branch to mirror here).
          if [[ "$GIT_TAG" == *"-rc."* ]]; then
            echo "IS_PRERELEASE=true" >> "$GITHUB_ENV"
          else
            echo "IS_PRERELEASE=false" >> "$GITHUB_ENV"
          fi

      # â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
      # STEP 2 â€” Build
      # Rebuilds the SAME tagged commit publish.yml already published from, so
      # the wheel/sdist signed below are bit-for-bit the ones published to PyPI.
      # poetry build â†’ twine check â†’ wheel purity verification
      # â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
      - name: "Step 2/6 â€” Build wheel and sdist"
        run: |
          pip install poetry twine
          poetry build

          echo "=== Build artifacts ==="
          ls -lh dist/

          echo "--- twine check ---"
          twine check dist/*

          echo "--- Wheel purity check ---"
          # Pure-Python wheel contains no compiled extensions
          for WHL in dist/*.whl; do
            echo "Checking: $WHL"
            python - "$WHL" <<'PYEOF'
          import zipfile, sys, os
          whl = sys.argv[1]
          with zipfile.ZipFile(whl) as z:
              names = z.namelist()
          exts = [n for n in names if n.endswith(('.so', '.pyd', '.dll'))]
          if exts:
              print(f"WARNING: compiled extensions found in wheel: {exts}", file=sys.stderr)
              # Not a hard failure â€” z3-solver ships native lib; pramanix itself is pure Python
          else:
              print(f"âœ“ Pure Python wheel confirmed: {os.path.basename(whl)}")
          PYEOF
          done

      # â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
      # STEP 3 â€” SBOM Generation (CycloneDX)
      # â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
      - name: "Step 3/6 â€” Generate CycloneDX SBOM"
        run: |
          pip install cyclonedx-bom

          # Install the package so cyclonedx-py can enumerate its deps
          pip install poetry
          poetry config virtualenvs.create false
          poetry install --extras all

          cyclonedx-py poetry \
            --output pramanix-sbom.cdx.json \
            --format json

          echo "=== SBOM summary ==="
          python - <<'PYEOF'
          import json
          with open("pramanix-sbom.cdx.json") as f:
              sbom = json.load(f)
          components = sbom.get("components", [])
          print(f"SBOM contains {len(components)} component(s)")
          # Verify key direct deps are present
          names = {c.get("name","").lower() for c in components}
          required = {"pydantic", "z3-solver", "structlog"}
          missing = required - names
          if missing:
              print(f"WARNING: expected components not found in SBOM: {missing}")
          else:
              print(f"âœ“ All required direct dependencies found in SBOM")
          PYEOF

      - name: Upload SBOM artifact
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02  # v4.6.2
        with:
          name: pramanix-sbom
          path: pramanix-sbom.cdx.json

      # â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
      # STEP 4 â€” Sigstore Artifact Signing
      # Signs .whl and .tar.gz; produces .sigstore.json bundles
      # Bundle proves: artifact hash + GitHub Actions workflow + commit SHA
      #
      # NOTE: PyPI publishing happens in publish.yml, not here (#322 fix) â€”
      # this job only re-builds the same tagged commit to sign and attach
      # SBOM/Sigstore evidence to the GitHub Release.
      # â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
      - name: "Step 4/6 â€” Sign artifacts with Sigstore"
        uses: sigstore/gh-action-sigstore-python@f514d46b907ebcd5bedc05145c03b69c1edd8b46  # v3.0.0
        with:
          inputs: >-
            dist/*.whl
            dist/*.tar.gz

      - name: Verify sigstore bundles were created
        run: |
          echo "=== Signed artifacts ==="
          ls -lh dist/
          # Confirm .sigstore.json bundles exist for each artifact
          for artifact in dist/*.whl dist/*.tar.gz; do
            bundle="${artifact}.sigstore.json"
            if [[ ! -f "$bundle" ]]; then
              echo "::error::Missing sigstore bundle for: $artifact"
              exit 1
            fi
            echo "âœ“ $bundle"
          done

      # â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
      # STEP 5 â€” GitHub Release
      # Extract CHANGELOG section for this version as release body
      # Attach: wheel, sdist, sigstore bundles, SBOM
      # â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
      - name: "Step 5/6 â€” Extract CHANGELOG section"
        run: |
          VERSION="${{ env.RELEASE_VERSION }}"
          # Extract the section between ## [VERSION] and the next ## heading
          NOTES=$(sed -n "/^## \[${VERSION}\]\|^## ${VERSION}/,/^## /{ /^## ${VERSION}\|^## \[${VERSION}\]/!{ /^## /d; p } }" CHANGELOG.md)
          if [[ -z "$NOTES" ]]; then
            NOTES="See [CHANGELOG.md](CHANGELOG.md) for details."
          fi
          # Write to file for the release action (avoids shell escaping issues)
          echo "$NOTES" > release-notes.md
          echo "=== Release notes preview ==="
          head -20 release-notes.md

      - name: "Step 5/6 â€” Create GitHub Release"
        uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65  # v2.1.0
        with:
          tag_name: ${{ env.GIT_TAG }}
          name: "Pramanix ${{ env.RELEASE_VERSION }}"
          body_path: release-notes.md
          prerelease: ${{ env.IS_PRERELEASE == 'true' }}
          files: |
            dist/*.whl
            dist/*.tar.gz
            dist/*.sigstore.json
            pramanix-sbom.cdx.json
          fail_on_unmatched_files: true

      # â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
      # STEP 6 â€” Post-Release Smoke Test
      # Clean ubuntu runner, pip install from PyPI, basic import.
      # publish.yml's own smoke-test step already confirmed installability
      # right after publishing; this is a second, independent confirmation
      # using the fully-built dist (not just `from pramanix import Guard`)
      # after CDN propagation has had more time to settle.
      # â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
      - name: "Step 6/6 â€” Post-release smoke test"
        run: |
          VERSION="${{ env.RELEASE_VERSION }}"

          echo "Waiting 120s for PyPI CDN propagation..."
          sleep 120

          # Create a clean virtual environment
          python -m venv /tmp/smoke-venv
          source /tmp/smoke-venv/bin/activate

          echo "--- Installing pramanix==$VERSION ---"
          pip install "pramanix==$VERSION"

          echo "--- Basic import smoke test ---"
          python -c "from pramanix import Guard, Policy, Field, E, Decision; print('OK')"

          echo "--- Installing pramanix[all]==$VERSION ---"
          pip install "pramanix[all]==$VERSION"

          echo "--- Full extras smoke test ---"
          python -c "
          from pramanix import Guard, Policy, Field, E, Decision
          from pramanix.translator import Translator, TranslatorContext
          print('pramanix[all] import OK')
          "

          echo "âœ“ Post-release smoke test passed for pramanix==$VERSION"

