# Pramanix SDK — Full Audit Report
**Date:** 2026-04-25
**Coverage at time of audit:** 98.65% (2865 passed, 1 skipped, 13 warnings — 134.96s)
**Auditors:** Viraj + deep-scan tooling
**Scope:** Source, tests, packaging, CI, deployment manifests

> **Reading this document:** Work top-to-bottom. Every row is a self-contained fix. The "Fix Action" column tells you exactly what to change. Tick the checkbox once the fix is merged and coverage is verified.

---

## Table of Contents
- [Pramanix SDK — Full Audit Report](#pramanix-sdk--full-audit-report)
  - [Table of Contents](#table-of-contents)
  - [1. Critical](#1-critical)
  - [2. High](#2-high)
  - [3. Medium](#3-medium)
  - [4. Low](#4-low)
  - [5. Packaging \& Configuration](#5-packaging--configuration)
  - [6. CI \& DevOps](#6-ci--devops)
  - [7. Test Suite Integrity](#7-test-suite-integrity)
  - [8. Cross-Cutting Issues](#8-cross-cutting-issues)
  - [9. Current Coverage Snapshot](#9-current-coverage-snapshot)
    - [Files Below 98%](#files-below-98)

---

## 1. Critical

| # | ID | File | Lines | Title | Description | Impact | Fix Action | Done |
|---|-----|------|-------|-------|-------------|--------|------------|------|
| 1 | C-01 | `guard.py` | `verify_async` finally block | **Resolver cache NOT cleared in `verify_async`** | `_resolver_registry.clear_cache()` is called in the sync `_verify_core` finally block but is **never called** in `verify_async`. Resolved field values from User A's request bleed directly into User B's next `verify_async` call on the same event loop. | **Cross-tenant data-bleed** in any async deployment. User A's resolved fields are visible to User B. | Add `_resolver_registry.clear_cache()` to `verify_async`'s own `finally` block, mirroring the sync path exactly. | ☑ |
| 2 | C-02 | `deploy/k8s/deployment.yaml` | Lines 111–116 | **Init container crashes on every deployment — two broken API calls** | (a) `Policy(name='warmup', invariants=lambda f: [E(f.x) >= 0])` — `Policy` must be subclassed, not instantiated with kwargs. Crashes with `TypeError: Policy() takes no arguments`. (b) `assert d.outcome in ('ALLOW', 'BLOCK')` — `Decision` has no `.outcome` field; the correct field is `d.status.value` or `d.allowed`. Neither `'ALLOW'` nor `'BLOCK'` are values `.status.value` ever returns. | **Every Kubernetes deployment fails.** Init container crashes on line 112, main container never starts, readiness probe never passes, rollout is permanently stuck. | (a) Replace inline `Policy(...)` with a proper subclass defined in the warmup script. (b) Replace `d.outcome` with `d.allowed` (bool) or `d.status.value` and update the assertion to match real enum values. | ☑ |
| 3 | C-03 | `pyproject.toml` / integration modules | N/A | **10 packages with full integration modules have no `[poetry.extras]` entry** | `cohere`, `mistralai`, `llama-cpp-python`, `google-generativeai`, `dspy`, `crewai`, `pydantic-ai`, `semantic-kernel`, `haystack-ai`, `asyncpg` are completely absent from `[tool.poetry.dependencies]` and no matching extras are defined. Error messages inside code (e.g. `cohere.py:100`) tell users to run `pip install 'pramanix[cohere]'` — but that extra does not exist; pip accepts the command silently and installs nothing. | Users of every non-OpenAI backend cannot install their required dependency via the SDK's own instructions. Silent install failure. | Define `[tool.poetry.extras]` entries: `cohere`, `mistral`, `gemini`, `llamacpp`, `postgres`, `dspy`, `crewai`, `pydantic-ai`, `semantic-kernel`, `haystack`. Add each package to the corresponding extra. | ☑ |

---

## 2. High

| # | ID | File | Lines | Title | Description | Impact | Fix Action | Done |
|---|-----|------|-------|-------|-------------|--------|------------|------|
| 1 | H-01 | `guard.py` | 765–794 | **`verify_async` skips `max_input_bytes` pre-check and fast-path evaluation** | The sync path enforces a max-input-bytes DoS guard and a field-presence / fast-path evaluation before invoking Z3. `verify_async` skips both entirely. | Async callers bypass the DoS guard. A large malicious payload only crashes the async path, not the sync path. Also breaks policy parity — fast-path short-circuits don't fire in async mode. | Port the `max_input_bytes` pre-check and the `_compiled_meta` field-presence / fast-path block from `_verify_core` into `verify_async` at the same logical position. | ☑ |
| 2 | H-02 | `guard.py` | 815–906 | **`verify_async` never calls `self._emit_to_sinks(decision)`** | Audit sinks are silently disabled for `async-thread` and `async-process` modes. A production deployment using either async mode produces **zero audit-log entries** without any warning. | Complete audit trail loss in async deployments. Compliance/forensic trail missing. | Call `self._emit_to_sinks(decision)` (or its async equivalent) from `verify_async` before returning, matching the sync path. Add an integration test asserting sink emission for all three execution modes. | ☑ |
| 3 | H-03 | `worker.py` | 745–757 | **`_dict_to_decision` silently drops 5 fields from worker results** | `_dict_to_decision` only reconstructs `allowed / status / violated_invariants / explanation / solver_time_ms / metadata`. Fields `intent_dump`, `state_dump`, `decision_hash`, `signature`, and `policy_hash` returned by the worker process are **silently discarded**. | Decisions from `async-process` mode have empty audit trail fields. Cryptographic signatures computed by the worker are thrown away — the audit log is unsigned for async-process mode. | Add all five missing fields to `_dict_to_decision`. Write a unit test asserting round-trip fidelity for every field `Decision` exposes. | ☑ |
| 4 | H-04 | `circuit_breaker.py` | 460–469 | **`_sync_state` silently maps unknown Redis state to `CLOSED` (fail-open)** | When the Redis backend returns an unexpected value, `except ValueError: synced = CircuitState.CLOSED`. An unknown/corrupted backend state is silently mapped to `CLOSED` (allow traffic) — fail-open behaviour in a component designed to fail-closed. | A Redis corruption or schema mismatch causes the circuit breaker to permanently allow traffic rather than halting it. | Replace silent `CLOSED` fallback with a logged warning + explicit `CircuitState.OPEN` (fail-safe), or raise a `CircuitBreakerBackendError` so callers can decide. | ☑ |
| 5 | H-05 | `circuit_breaker.py` | 716–730 | **`RedisDistributedBackend.clear` crashes inside running event loop** | Uses deprecated `asyncio.get_event_loop()` + `loop.run_until_complete`. Called from inside an already-running event loop → `RuntimeError`. `DistributedCircuitBreaker.reset()` has the same bug. | `reset()` is unusable in async contexts (FastAPI, async tests, etc.). | Refactor to `asyncio.run()` (if guaranteed no loop) or expose a proper `async def clear_async()` and call it with `await`. Remove all `get_event_loop()` uses. | ☑ |
| 6 | H-06 | `audit/archiver.py` | 233–275 | **Archive write is not atomic — crash produces corrupt archive** | Archive is written with `path.write_text(...)` — no `fsync`, no rename-from-temp. A crash mid-write produces a permanently corrupt archive. Additionally, the active-chain clear is not atomic with the archive write; a crash between the two leaves state inconsistent with no recovery path. | Permanent data loss / corrupt compliance archive on any mid-write process crash. | Implement: write to `.tmp` → `f.flush()` → `os.fsync(f.fileno())` → `os.replace(tmp, final)`. Wrap both the write and the chain-clear in the same atomic operation (or use a journal). | ☑ |
| 7 | H-07 | `audit/merkle.py` | 128–134 | **Merkle tree uses last-leaf duplication (Bitcoin CVE-style attack)** | Merkle tree uses last-leaf duplication for odd-length levels. Two distinct leaf sequences can produce the same root hash — the same vulnerability as Bitcoin's 2012 CVE. For financial/medical/compliance audit trails this is unacceptable. | An attacker can construct two different audit log sequences that hash to the same Merkle root, making tampering undetectable. | Prefix real leaf nodes with `\x00` and internal (padded) nodes with `\x01` before hashing. This is the standard mitigation for the duplication attack. Add a test with an odd-length leaf set proving the roots differ from a crafted duplicate sequence. | ☑ |
| 8 | H-08 | `audit_sink.py` | 167–189 | **`_queue_depth` is a plain `int` incremented across two threads — TOCTOU race** | `_queue_depth` is incremented in the emit thread and decremented in the Kafka I/O delivery callback thread. No lock. Two threads can simultaneously pass the `>= max_queue` check and both produce, exceeding the configured queue limit. | Kafka queue overflow; memory growth unbounded under high load; queue depth counter permanently incorrect. | Protect `_queue_depth` with a `threading.Lock` (or use `threading.Semaphore`). Alternatively use an `asyncio.Queue` with `maxsize` for the async path. | ☑ |
| 9 | H-09 | `audit_sink.py` | 274–286 | **`S3AuditSink.emit` blocks the event loop with a synchronous `boto3` call** | `boto3.put_object` is called synchronously inside `guard.verify` or on the asyncio event loop. May block 100 ms–30 s per call. | Latency spikes / full event-loop stall on every audit event in async mode. Under load, this halts all concurrent verifications. | Wrap in `asyncio.get_event_loop().run_in_executor(None, ...)` or use `aioboto3`. Add a configurable timeout. | ☑ |
| 10 | H-10 | `audit_sink.py` | 308–349 | **`SplunkHecAuditSink` uses synchronous `urllib.request.urlopen` on the event loop** | Same blocking problem as H-09. Also has no CA-bundle option for private TLS deployments. | Event-loop stall on every Splunk emit; private CA deployments cannot verify TLS. | Replace with `httpx.AsyncClient` (already a dependency). Add a `ca_bundle` parameter. | ☑ |
| 11 | H-11 | `execution_token.py` | 968–982 | **`_async_consume` opens a new `asyncpg` connection per token — no pool** | `await asyncpg.connect(self._dsn)` is called on every token consumption. No connection pool. | Saturates Postgres connection limit under any real load. `max_connections` hit → all token verifications fail. | Replace with `asyncpg.create_pool()` initialised once in `__init__` / `async def setup()`. Use `pool.acquire()` per call. | ☑ |
| 12 | H-12 | `key_provider.py` | 270–277, 340–345, 408–414, 472–479 | **All KMS providers make a live network call on every invocation — no caching** | `AwsKmsKeyProvider.private_key_pem()`, Azure, GCP, and Vault equivalents all make a live network call on every invocation (50–300 ms + API charges). Decision-signing issues one call; `key_version()` issues another. No TTL cache. | Under load, cloud API costs double and latency increases by 100–600 ms per decision. Risk of KMS rate-limit throttling. | Add a `functools.lru_cache` or TTL cache (e.g. `cachetools.TTLCache`) per provider with a configurable TTL (default 300 s). Refresh on `rotate_key()`. | ☑ |
| 13 | H-13 | `integrations/langchain.py`, `integrations/llamaindex.py` | 87–89, 234–236, 462–464 | **`ThreadPoolExecutor(max_workers=1)` constructed per call — unbounded thread leak** | A new `ThreadPoolExecutor` is created inside the call handler on every invocation. Executors are never shut down. | Unbounded thread creation under load; OS thread limit exhaustion; memory leak over process lifetime. | Create the executor once in `__init__` (or at class level) and reuse it. Call `executor.shutdown(wait=False)` in `__del__` / a `close()` method. | ☑ |
| 14 | H-14 | `integrations/fastapi.py`, `integrations/langchain.py`, `integrations/autogen.py` | Various | **`model_validate(strict=False)` in middleware vs `strict=True` in Guard — silent semantic drift** | Integration adapters use `model_validate(raw, strict=False)` while `pramanix.validator` uses `strict=True`. The same intent dict can validate in middleware and fail in Guard, or vice versa. | Silent policy bypass or unexpected rejections depending on entry point. Behaviour differs across integration surfaces with no warning. | Centralise validation into a single shared helper that always uses `strict=True`. Remove all per-integration `model_validate` calls. | ☑ |
| 15 | H-15 | `interceptors/grpc.py` | Various | **Only `unary_unary` RPC type is wrapped — all streaming RPCs bypass the guard** | Only `unary_unary` interceptor is implemented. `unary_stream`, `stream_unary`, and `stream_stream` RPCs pass through completely unguarded while appearing to be protected. | Any gRPC service with streaming methods is silently unprotected. Security theatre — the interceptor gives false confidence. | Implement `unary_stream`, `stream_unary`, and `stream_stream` interceptor methods, each calling the guard on the appropriate message. | ☑ |
| 16 | H-16 | `translator/redundant.py` | 293–304 | **`injection_scorer_path` loaded via `exec_module` — arbitrary code execution if env var is attacker-controlled** | `injection_scorer_path` is loaded via `spec_from_file_location` + `exec_module`. If an attacker controls `PRAMANIX_INJECTION_SCORER_PATH` or the file path, they get arbitrary code execution on the first verification call. | Remote code execution at process startup / first verify call. | Remove `exec_module` dynamic loading. Ship the scorer as a proper package module. If extensibility is required, use a restricted plugin interface (e.g. importlib entry points) with path validation and signature verification. | ☑ |
| 17 | H-17 | `pyproject.toml` | line 45, 97 | **`anthropic = "^0.25"` is incompatible with `httpx >= 0.28.1`** | Both the optional and dev dependency pin `anthropic = "^0.25"`. Version 0.25.x is incompatible with `httpx >= 0.28.1` (the `proxies` kwarg was removed in httpx 0.28). Every fresh `pip install -e .[dev]` installs a broken version and fails on any Anthropic test. | CI and contributor environments are broken on a clean install. Every Anthropic backend test fails. | Bump the pin to `anthropic = "^0.97"` (the running venv already has 0.97.0). Run `poetry lock` and commit the updated lockfile. | ☑ |
| 18 | H-18 | `deploy/k8s/deployment.yaml` | 34, 64, 103, 133 | **K8s manifests hardcoded to image `pramanix:0.8.0` while package is at `1.0.0`** | All image references and version labels in the deployment manifest are hardcoded to `0.8.0`. `pyproject.toml` declares `version = "1.0.0"`. There is no templating (`{{ .Values.image.tag }}`). The manifest cannot be used without manual editing on every release. | Operators unknowingly deploy stale images. No automated version synchronisation. | Replace hardcoded version strings with Helm/Kustomize variables or a `make release` step that patches the manifest from `pyproject.toml`. | ☑ |
| 19 | H-19 | `tests/` | Various | **210 `sys.modules` injections + 257 `MagicMock`/`AsyncMock` usages — test suite tests fake infrastructure** | Core backends (Kafka, S3, asyncpg, llama.cpp, Vault, Azure KeyVault, Gemini, Cohere) are systematically replaced with fake objects that never reject, never timeout, and never return malformed responses. The SDK's core guarantee ("every ALLOW has a mathematical proof") cannot be meaningfully tested with fake transports. | Critical error/security paths (delivery failures, connection timeouts, malformed LLM responses) are untested in any realistic scenario. | Replace `sys.modules` injection with real Docker-compose or testcontainers fixtures for Kafka, Postgres, Redis. Use `respx` / `httpretty` for HTTP backends. Migrate integration tests to `tests/integration/` with proper extras installed in CI. | ☐ |
| 20 | H-20 | `.github/workflows/ci.yml` | line 91 | **`pip-audit --skip-editable` never scans the `pramanix` package itself** | `--skip-editable` skips `pramanix` because it is installed in editable mode. pip-audit scans all third-party deps but never pramanix's own declared dependencies as resolved for the specific version being tested. | A new pramanix optional dependency that introduces a vulnerable transitive dep passes CVE scanning silently. | Remove `--skip-editable`. If editable mode causes issues, run `pip install .` (non-editable) in the audit step. | ☑ |

---

## 3. Medium

| # | ID | File | Lines | Title | Description | Impact | Fix Action | Done |
|---|-----|------|-------|-------|-------------|--------|------------|------|
| 1 | M-01 | `guard.py` | 669–683 | **`Decision.error(reason=str(exc))` leaks verbose error text when `redact_violations=True`** | Error reasons remain fully verbose even when `redact_violations=True`. An oracle attacker can probe field structure via Z3 timeout messages. | Information leakage of internal policy/field structure to callers. | When `redact_violations=True`, replace error reason with a generic string (e.g. `"verification_error"`) and emit the verbose reason only to the audit sink. | ☑ |
| 2 | M-02 | `guard.py` | 1012–1025 | **`Guard.shutdown()` does not close translator HTTP clients — FD/socket leak** | `httpx.AsyncClient`, `cohere.AsyncClient`, and similar HTTP clients are not closed in `Guard.shutdown()`. | Socket and file-descriptor leak over process lifetime. In long-running services this eventually exhausts OS limits. | Add `await translator.aclose()` (or equivalent) for each translator in `Guard.shutdown()`. Consider `__aexit__` / `contextlib.asynccontextmanager` for the Guard lifecycle. | ☑ |
| 3 | M-03 | `guard.py` | 125–133 | **`_is_picklable` swallows all exceptions, discarding `PicklingError` reason** | `except Exception: ...` discards the specific `PicklingError` reason. | Diagnostic signal lost; developers cannot tell what made a model un-picklable. | Change to `except PicklingError as e: logger.warning("Not picklable: %s", e); return False`. | ☑ |
| 4 | M-04 | `circuit_breaker.py` | 425–435 | **`DistributedCircuitBreaker` registers no Prometheus metrics** | The distributed variant has zero observability vs the single-node variant which has full Prometheus instrumentation. | Operators cannot monitor the distributed circuit breaker. Open/closed state transitions are invisible. | Add the same Prometheus `Counter` / `Gauge` metrics to `DistributedCircuitBreaker` that exist on the single-node version. | ☑ |
| 5 | M-05 | `circuit_breaker.py` | 195–200 | **State check and guard call not under the same lock — two coroutines can race through `CLOSED`** | State check and guard invocation are not atomic. Two coroutines racing through `CLOSED` can both be in-flight when one trips the breaker. | The breaker can allow more requests than the threshold during the race window. | Wrap the state-check + increment in a single `asyncio.Lock` acquisition. | ☑ |
| 6 | M-06 | `worker.py` | 668–686 | **Magic number `9999.0` used to fake P99 latency on failures** | `9999.0` is used as a sentinel to fake P99 latency for failed dispatches. No named constant; meaning is invisible. | Code readability and maintainability issue; misleads anyone reading metrics. | Replace with `_FAILED_DISPATCH_PENALTY_MS: float = 9999.0` at module level. | ☑ |
| 7 | M-07 | `worker.py` | 711–743 | **`_recycle()` log line formats both `%d` placeholders with `max_decisions_per_worker`** | The first `%d` should be `self._counter`; the second should be `max_decisions_per_worker`. Every recycle log entry reports the wrong counter value. | Incorrect operational logs; workers appear to never increment their decision counter. | Fix the log format call: `logger.info("recycling after %d / %d decisions", self._counter, max_decisions_per_worker)`. | ☑ |
| 8 | M-08 | `worker.py` | 212–261 | **`_ppid_watchdog` Windows branch uses `GetCurrentProcessId()` — gets child PID, not parent's** | The Windows watchdog is supposed to watch the parent process PID. `GetCurrentProcessId()` returns the child's own PID, not the parent's. The Windows watchdog is permanently broken. All watchdog lines are `# pragma: no cover`. | Child processes on Windows are never reaped if the parent dies; zombie/orphan worker processes accumulate. | Use `os.getppid()` (available on Windows via Python 3.8+) or pass the parent PID explicitly at spawn time. Remove `# pragma: no cover` and add a test. | ☑ |
| 9 | M-09 | `transpiler.py` | 619–677 | **`InvariantASTCache` is implemented and exported but never used by `Guard.__init__`** | `Guard.__init__` calls `_compile_policy()` directly without consulting `InvariantASTCache`. The cache is never populated by the SDK itself. Dead public API. Additionally, the cache keys on `id(policy_cls)`, which can be reused after GC with dynamically created policies. | Unnecessary recompilation of invariants on every `Guard` construction; published public API that silently does nothing; potential cache collisions with dynamic policies. | Wire `InvariantASTCache` into `_compile_policy()`. Change the cache key to include the class's `__qualname__` + `__module__` + invariant hash, not `id()`. | ☑ |
| 10 | M-10 | `translator/cohere.py` | 165–181 | **Fallback for older Cohere SDK uses `get_event_loop()`, sync client in async function, and different prompt schema** | Older Cohere SDK fallback: uses deprecated `asyncio.get_event_loop()`, uses a sync client inside an async function, and sends a structurally different prompt schema to Cohere. | Silent behavioural divergence between SDK versions; event-loop blocking; two distinct prompt schemas means two different extraction semantics in production. | Drop support for the older Cohere SDK version or gate it explicitly. Consolidate to a single code path using `asyncio.run()` or `await`. | ☑ |
| 11 | M-11 | `translator/cohere.py` | 105–114 | **`except AttributeError` falls back to `_retryable = (Exception,)` — retries all exceptions** | On `AttributeError`, retryable exception set expands to `(Exception,)`, which retries `KeyError`, `TypeError`, and programmer errors. Wastes Cohere API budget. | Unnecessary API spend; genuine programming errors are silently retried rather than surfaced. | Enumerate specific Cohere transport exceptions in the fallback (e.g. `cohere.errors.CohereTooManyRequestsError`). Never retry `KeyError` / `TypeError`. | ☑ |
| 12 | M-12 | `translator/gemini.py` | 107–108 | **`genai.configure(api_key=...)` mutates global module state** | Two `Guard` instances using different Gemini API keys will overwrite each other's key on concurrent requests. | Multi-tenant Gemini usage is broken; requests from Guard A use Guard B's API key after a race. | Use per-instance `genai.Client(api_key=...)` if the SDK supports it, or enforce a single global key via a class-level lock with a warning if multiple keys are detected. | ☑ |
| 13 | M-13 | `translator/mistral.py` | 117–141 | **Retry block uses `retry_if_exception_type(Exception)` — retries programmer errors; silent exhaustion** | All exceptions including `KeyError`, `TypeError` are retried. With `reraise=False`, exhaustion exits silently returning `raw=None`. | Programmer errors burn the full retry budget silently. `None` propagates downstream as if it were a valid response. | Restrict retryable types to Mistral transport exceptions. Set `reraise=True` or explicitly raise after exhaustion. | ☑ |
| 14 | M-14 | `translator/mistral.py` | 161–178 | **New Mistral client created per retry attempt — no connection reuse** | A new `MistralClient` is constructed inside the retry loop on every attempt. | TCP+TLS re-established per retry; latency increases compound; no connection pooling. | Construct the client once in `__init__` and reuse it across calls and retries. | ☑ |
| 15 | M-15 | `audit_sink.py` | 188–189 | **`poll(0)` only called inside `emit()` — delivery callbacks stall under idle/bursty patterns** | A dedicated delivery-callback thread is not started. Kafka callbacks only fire when `emit()` is called; under bursty or idle patterns callbacks stall and `_queue_depth` drifts. | Kafka delivery confirmations are delayed or never processed; queue depth counter incorrect; messages appear undelivered. | Start a background thread calling `producer.poll(timeout=1)` in a loop, or use Kafka's background thread model (`flush()` on a schedule). | ☑ |
| 16 | M-16 | `audit_sink.py` | 396–420 | **`DatadogAuditSink` creates a new `ApiClient` + `LogsApi` on every `emit()` call** | A new `ApiClient` and `LogsApi` object are constructed inside `emit()` on every invocation. | Object allocation overhead per emit; connection not reused; no rate-limit awareness per client. | Construct `ApiClient` and `LogsApi` once in `__init__`. Close the client in a `close()` / `__del__` method. | ☑ |
| 17 | M-17 | `execution_token.py` | 715–742 | **`RedisExecutionTokenVerifier.consume()` docstring contradicts implementation** | Docstring says it raises `redis.RedisError` on connection failure; code catches `Exception` and returns `False`. Callers who catch `redis.RedisError` to trigger a retry will never see one. | Silent token consumption failure; callers cannot implement retry logic as documented. | Either propagate `redis.RedisError` (update code to match docs) or update the docstring to describe the `False` return (update docs to match code). Prefer the former. | ☑ |
| 18 | M-18 | `execution_token.py` | 887–921 | **`PostgresExecutionTokenVerifier._run` re-invents an event loop per call — unbounded threads** | Uses `asyncio.get_event_loop()` (deprecated) to run async code. Under load this spawns an unbounded number of threads. | Thread exhaustion under concurrent token verifications; `DeprecationWarning` storm in Python 3.12+. | Refactor to be a native `async def` and `await` it, or use `asyncio.run()` in a thread-pool with bounded concurrency. | ☑ |
| 19 | M-19 | `k8s/webhook.py` | 100–101 | **Raw exception text included in K8s API server response** | `content=f"Pramanix webhook error: {exc}"` puts raw exception text in the K8s API server response. Pydantic `ValidationError` text includes field values. Also accepts POST from any source — no mutual-TLS enforcement. | Internal field names/values leak to K8s controllers. No authentication on the webhook endpoint. | Catch exceptions, log verbosely, and return a generic message. Add mTLS client certificate validation. | ☑ |
| 20 | M-20 | `integrations/crewai.py` | 138–143 | **Raw exception text returned to agent response** | `return f"Guard error during verification: {exc}"` exposes raw exception text to the CrewAI agent response. Marked `# pragma: no cover`. | Internal error details visible to agent/LLM output; also never tested. | Return a generic string. Log the exception with full context. Remove `# pragma: no cover` and add a test. | ☑ |
| 21 | M-21 | `integrations/haystack.py` | 71–77 | **`component(self.__class__)` called per-instance — mutates class each time** | `component()` should be called at class-definition time (as a class decorator), not in `__init__` per instance. Called per-instance it mutates the class on every instantiation. | Unexpected class-level side effects on every `HaystackGuardedComponent` instantiation; potential descriptor corruption. | Move `component(HaystackGuardedComponent)` to class definition level (apply as decorator). | ☑ |
| 22 | M-22 | `integrations/haystack.py` | 109–126 | **All exceptions in `run()` silently funnel to `blocked_docs` — no log, no metric** | `except Exception:` swallows all errors and marks documents as blocked. No log, no metric emitted. | Legitimate bugs (e.g. a Z3 crash, network timeout) are indistinguishable from a policy BLOCK. Operators have no visibility. | Log the exception at ERROR level with full context. Emit a Prometheus counter for unexpected errors vs policy blocks. | ☑ |
| 23 | M-23 | `integrations/semantic_kernel.py` | Various | **Sync `verify()` called from inside SK async kernel — blocks the event loop; raw exception to planner** | Sync `verify()` blocks the event loop when called from async SK context. `return json.dumps({"error": str(exc)})` leaks raw exception text to the SK planner. | Event-loop blocking; internal error details exposed to the LLM planner. | Use `await guard.verify_async(...)`. Catch exceptions and return a generic structured error object. | ☑ |
| 24 | M-24 | `integrations/llamaindex.py` | 44–60 | **Placeholder `ToolMetadata` / `ToolOutput` dataclasses exported as real types when `llama_index` not installed** | Code importing the types (not instantiating) silently gets the fake stub versions instead of the real `llama_index` types. | Silent type mismatch; code that imports and type-checks against these classes behaves differently with and without `llama_index` installed. | Use `TYPE_CHECKING` guards. Raise `ImportError` with a helpful message on import if `llama_index` is absent. Do not export stub types at all. | ☑ |
| 25 | M-25 | `policy.py` | line 244 | **`except NotImplementedError: own = []` silently empties invariant list on MRO errors** | If any MRO ancestor raises `NotImplementedError`, the invariants list silently becomes `[]`. `Guard` validation raises `PolicyError` but the MRO-walk failure is invisible. | A policy with a buggy parent class silently validates with zero invariants — guard is effectively disabled. | Log the `NotImplementedError` at WARNING and re-raise as `PolicyConfigurationError` so the problem surfaces at Guard construction time. | ☑ |
| 26 | M-26 | `helpers/policy_auditor.py` | 165–169 | **`except Exception: return set()` when `invariants()` raises — CI passes for broken policy** | If `invariants()` raises, the auditor reports every field as uncovered silently. CI passes even for a policy with a broken `invariants()`. | Coverage reports are silently wrong; broken policies ship to production. | Re-raise or surface the exception as an auditor finding. At minimum, log at ERROR and include the exception in the audit report output. | ☑ |
| 27 | M-27 | `helpers/serialization.py` | 156–164 | **Picklability check guarded by `if __debug__` — disabled in production with `python -O`** | In production (where Python is often run with `-O`), the picklability check is skipped. A non-picklable model passes dev checks and fails at process-pool dispatch time in production. | Silent production failure when deploying models that are not picklable. Dev and production environments have different validation behaviour. | Remove the `if __debug__` guard. Always run the picklability check at Guard construction. | ☑ |
| 28 | M-28 | `key_provider.py` | 124, 168, 215 | **Three of seven `KeyProvider` implementations raise `NotImplementedError` from `rotate_key()`** | The `Protocol` declares `rotate_key()` as required. Three concrete providers raise `NotImplementedError`. Generic callers crash at runtime; no `supports_rotation` guard. | `rotate_key()` unusable for AWS, Azure, GCP providers without knowing which providers support it. | Add a `supports_rotation: bool` property to the `Protocol`. Set to `False` in providers that don't implement it. Guard callers with `if provider.supports_rotation:`. | ☑ |
| 29 | M-29 | `translator/_sanitise.py` | Various | **Injection confidence `recipient_id` heuristic blocks every Unicode name and email** | Pattern `[^a-zA-Z0-9_\\-]` blocks any Unicode character in IDs. Any non-ASCII name (e.g. `André`, `김민준`) or email-format ID fails the heuristic. | SDK is broken for non-English/non-ASCII tenants. | Expand the pattern to allow Unicode word characters (`\w` with `re.UNICODE`), or apply the heuristic only to known-ASCII fields. | ☑ |
| 30 | M-30 | `decision.py` | Various | **`SolverStatus.CACHE_HIT` exported but `Decision.cache_hit(...)` factory does not exist** | `SolverStatus.CACHE_HIT` is exported, documented, and referenced in type annotations, but no `Decision.cache_hit(...)` factory exists anywhere in the SDK. Dead public API. | Callers following the documentation pattern get `AttributeError` at runtime. | Either implement `Decision.cache_hit(...)` or remove `SolverStatus.CACHE_HIT` from the public API and documentation. | ☑ |
| 31 | M-31 | `resolvers.py` | `register()` | **`register()` silently overwrites existing resolver with only a WARNING log** | A duplicate resolver registration is silently accepted. The previous resolver is lost with only a WARNING. | Two different policy modules accidentally registering the same field name silently discard one resolver. | Require `force=True` to overwrite an existing resolver. Raise `ResolverConflictError` by default. | ☑ |
| 32 | M-32 | `migration.py` | `migrate()` | **`migrate()` silently skips `field_renames` keys not present in state — no strict mode** | Missing keys are silently skipped. A migration that renames a field that doesn't exist in the target state produces no error or warning. | Silent migration failures; state is partially migrated; no feedback to the operator. | Add a `strict: bool = False` parameter. When `strict=True`, raise `MigrationError` for missing keys. | ☑ |
| 33 | M-33 | `pyproject.toml` | ruff config | **`ruff target-version = "py312"` contradicts `python = ">=3.13"`** | The package declares `python = ">=3.13"` but ruff lints against `"py312"`. Ruff misses 3.13-specific upgrades and silently suggests backports that don't apply to a 3.13-only package. | Incorrect lint baseline; 3.12-only patterns not flagged for removal; 3.13 idiomatic patterns not suggested. | Change `ruff` → `[tool.ruff] target-version = "py313"` in `pyproject.toml`. | ☑ |
| 34 | M-34 | `pyproject.toml` | coverage config | **`fail_under = 95` while team targets 98% — CI gate too low** | A PR that drops coverage from 97.5% to 95.1% passes CI without any signal. | Coverage can silently degrade below the team's own stated target. | Set `fail_under = 98` in `pyproject.toml`. | ☑ |
| 35 | M-35 | `pyproject.toml` | coverage config | **`exclude_lines = ["raise NotImplementedError"]` hides untested concrete `rotate_key()` raises** | This exclusion is correct for abstract base methods but silently exempts six concrete `rotate_key()` raises in `key_provider.py` that are supposed to be implemented. Those paths have never been tested. | Untested concrete provider methods; coverage gate passes despite zero test coverage of key rotation. | Scope the exclusion to `# pragma: abstract` comments on genuinely abstract methods. Remove the blanket `raise NotImplementedError` exclusion. | ☑ |
| 36 | M-36 | Various | 86 annotations | **86 `# pragma: no cover` annotations — 5 exempt actual error/security paths** | The following are indefensible: `crypto.py:242–244` (signing failure → empty string — audit integrity), `interceptors/kafka.py:159,165` (DLQ produce failure + commit failure — message loss), `integrations/crewai.py:139` (raw exc to agent), `translator/injection_filter.py:211` (fail-open exception path), `worker.py:352` (`_warmup_worker` silent failure). | Critical security and data-loss paths never tested. | Remove these 5 `# pragma: no cover` annotations and write tests that exercise them. | ☑ |
| 37 | M-37 | `pyproject.toml` | line 302 | **`openai_compat.py` permanently excluded from coverage gate despite being a full implementation** | `openai_compat.py` is excluded from coverage with the comment "not installed in base dev env" — but `openai` IS in dev dependencies and IS installed. The retry path, `_single_call`, and error handling are completely untested. | The most common translator backend (OpenAI, Azure OpenAI, vLLM, LMStudio) has zero test coverage. | Remove the exclusion from `pyproject.toml`. Add tests for `_single_call`, retry exhaustion, and `OpenAICompatTranslator.extract()`. | ☑ |
| 38 | M-38 | CI | `tests/integration/` | **Integration test suite is structurally dead in standard CI** | `tests/integration/` tests require extras that are never installed in CI (see C-03). The entire integration suite is structureless in standard CI runs. Coverage gate only covers `tests/unit/` in practice. | Integration contracts between the SDK and real backends are never validated in CI. | Add a CI job that installs all extras and runs `tests/integration/` with testcontainers. | ☑ |
| 39 | M-39 | `setup.cfg` | `[flake8]` section | **`[flake8]` config in `setup.cfg` but `flake8` is not a dependency — dead config** | The project uses `ruff`. The `flake8` config in `setup.cfg` contradicts ruff settings and will confuse contributors who run `flake8` locally. | Contributor confusion; contradictory linter config; contributors may get false-clean results from flake8. | Remove `[flake8]` from `setup.cfg` entirely, or add a note that ruff is the linter. | ☑ |
| 40 | M-40 | `pyrightconfig.json` | `typeCheckingMode` | **Pyright `basic` vs mypy `strict` — IDE shows clean state that doesn't match CI** | pyright runs in `basic` mode (IDE), mypy runs with `strict = true` (CI). Pyright misses `reportMissingParameterType`, `reportReturnType`, etc. Developers see a clean squiggle state that CI rejects. | Developer experience friction; type errors only discovered at CI stage, not during editing. | Change `pyrightconfig.json` to `"typeCheckingMode": "strict"` to match mypy. | ☑ |
| 41 | M-41 | `pyproject.toml` ruff | `select` list | **Ruff lint rules missing `S` (security) and `ASYNC` (async anti-patterns)** | The `ruff select` list does not include `S` (flake8-bandit) or `ASYNC`. `pickle.load` in `injection_scorer.py`, all `asyncio.get_event_loop()` calls, and bare `except` blocks are not caught by the linter. | Security issues and async anti-patterns pass linting silently. | Add `"S", "ASYNC"` to `[tool.ruff.lint] select`. Fix or `# noqa`-annotate each new finding. | ☑ |
| 42 | M-42 | `Dockerfile.production` | `RUN pip install` | **Production Docker image installs from PyPI without a lockfile — non-reproducible builds** | `RUN pip install --no-cache-dir "pramanix[all]==${VERSION}"` has no pinned transitive dependencies. The same `VERSION` built on two different days may install different transitive dep versions. | Non-reproducible production images; a transitive dep update can silently change production behaviour or introduce vulnerabilities. | Use `pip install --require-hashes` with a pre-generated `requirements.txt` produced by `pip-compile --generate-hashes`. | ☑ |
| 43 | M-43 | Cross-cutting | 5 files | **`asyncio.get_event_loop()` deprecated pattern in 5 files** | Appears in `circuit_breaker.py:725`, `execution_token.py:893`, `cohere.py:172`, `gemini.py:164`, `llamacpp.py:110`. Emits `DeprecationWarning` in Python 3.12+; raises in a future Python version. | `DeprecationWarning` storm; will become a hard `RuntimeError` in a future Python release. | Replace with `asyncio.run()` (for sync contexts) or `asyncio.get_running_loop()` (inside async contexts). See per-file issues above. | ☑ |
| 44 | M-44 | Cross-cutting | Multiple | **`str(exc)` leaks raw exception text into responses** | Appears in `crewai.py`, `semantic_kernel.py`, `k8s/webhook.py`, `grpc.py`, `fastapi.py`, `dspy.py`. | Internal error details (field names, stack fragments) exposed to callers and LLM agents. | Centralise error formatting: log verbose exception internally, return a generic structured error to callers. | ☑ |
| 45 | M-45 | Cross-cutting | All translators | **No circuit breaker protecting translator calls** | A slow-but-not-timed-out LLM backend can stall the entire `parse_and_verify` path indefinitely. | Complete stall of all verification under a degraded LLM backend. | Add a `CircuitBreaker` (the SDK already has one) wrapping each translator's `extract()` call. | ☑ |
| 46 | M-46 | Cross-cutting | All translators | **No Prometheus metric for LLM-specific failures (`CONSENSUS_FAILURE`, `EXTRACTION_FAILURE`)** | Only Z3 outcomes are counted in Prometheus. LLM-side failures are invisible in dashboards. | Operators cannot distinguish policy rejections from LLM extraction failures. | Add `Counter` metrics for `extraction_failure_total`, `consensus_failure_total` labelled by translator name. | ☑ |
| 47 | M-47 | `decision.py` | `__hash__` | **`Decision.__hash__` crashes at runtime — `intent_dump`/`state_dump`/`metadata` are unhashable dicts** | `Decision` is a frozen dataclass. Its auto-generated `__hash__` includes `dict` fields (`intent_dump`, `state_dump`, `metadata`) which are unhashable. | Any code that puts a `Decision` in a `set` or uses it as a dict key raises `TypeError` at runtime. | Override `__hash__` to hash only the hashable fields (`decision_hash`, `status`, `allowed`), or convert dict fields to `frozenset` / `tuple` in `__post_init__`. | ☑ |
| 48 | M-48 | Cross-cutting | `WorkerPool`, `RedisDistributedBackend`, `PramanixKafkaConsumer` | **No `__del__` — GC without explicit `shutdown()` leaks processes, sockets, threads** | If callers forget to call `shutdown()`, GC does not release worker processes, Redis connections, or Kafka threads. | Resource leak in long-running services or tests that construct and discard Guard instances. | Add `__del__` methods that call `shutdown()` with a warning log. Document that explicit `shutdown()` is strongly preferred. | ☑ |
| 49 | M-49 | `crypto.py` | `sign()` | **`sign()` returns `""` on any error — no metric for systematic signing failures** | Fail-closed behaviour is correct, but there is no structured metric. Operators cannot detect systematic signing failures. | Silent audit-trail integrity failure; all decisions appear unsigned with no alert. | Add a Prometheus `Counter` `pramanix_signing_failure_total`. Emit on every `sign()` error. | ☑ |
| 50 | M-50 | `decision.py` | `_compute_hash` | **`_compute_hash` uses `metadata.get("policy", "")` but `policy_hash` is a top-level field** | Canonical bytes are inconsistent with the field layout. Audit replay tooling that tries to verify `decision_hash` will get a different result because it reads the correct `policy_hash` field. | Audit replay / tamper detection is broken. Canonical hash cannot be reproduced from the `Decision` object itself. | Update `_compute_hash` to use `self.policy_hash` directly instead of `self.metadata.get("policy", "")`. | ☑ |
| 51 | M-51 | `ci.yml` | line 62 | **CI comment references `CVE-2026-1703` for pip 25.3 — nonexistent CVE** | `pip 25.3` and `CVE-2026-1703` do not exist in the public CVE database as of 2026-04-25. This is either a fabricated CVE reference or was written prospectively. | Misleads contributors and auditors; CI justification for a mandatory upgrade step is fictitious. | Remove the CVE reference. Use a plain comment: `# Always use the latest pip for security patches`. | ☑ |

---

## 4. Low

| # | ID | File | Lines | Title | Description | Fix Action | Done |
|---|-----|------|-------|-------|-------------|------------|------|
| 1 | L-01 | `guard.py` | 236–237 | **No-op `try/except` — `try: ... except Exception: raise`** | The try block catches and immediately re-raises. No logging occurs. This is dead code. | Drop the `try/except` entirely, or add a `logger.debug(...)` before `raise` if context is needed. | ☑ |
| 2 | L-02 | `guard.py` | 416–453 | **Three-level size fallback (JSON → `repr` → BLOCK) adds complexity without value** | `repr()` size is not a meaningful security bound. The middle fallback layer adds code complexity without adding protection. | Remove the `repr()` fallback level. If JSON serialisation fails, fall directly to BLOCK. | ☑ |
| 3 | L-03 | `guard.py` | 339–346 | **`contextlib.suppress(InterruptedError, OSError)` in min-response-ms sleep loop is overbroad** | `OSError` suppression is too broad — it suppresses legitimate OS-level errors beyond interrupted sleeps. | Change to `contextlib.suppress(InterruptedError)` only. | ☑ |
| 4 | L-04 | `solver.py` | line 297 | **`del s` comment claims to prompt Z3 memory release — misleading** | `del` only decrements refcount; CPython's GC decides when native Z3 memory is freed. Comment misleads maintainers. | Replace `del s` with `s.reset()` for explicit Z3 memory release, and update the comment accordingly. | ☑ |
| 5 | L-05 | `worker.py` | 264–355 | **`except Exception: pass` in `_warmup_worker` — silent Z3 failure** | If Z3 is broken, warmup silently does nothing. No log, no metric. | Add `logger.error("Z3 warmup failed: %s", exc, exc_info=True)` and emit a metric. Remove `# pragma: no cover`. | ☑ |
| 6 | L-06 | `worker.py` | 524–565 | **`_lock = field(init=False, default_factory=threading.Lock)` immediately overwritten in `__post_init__`** | Dead `field()` init — `__post_init__` overwrites it immediately. | Remove the `field(...)` declaration; assign only in `__post_init__`. | ☑ |
| 7 | L-07 | `audit_sink.py` | 110–113 | **`InMemoryAuditSink.emit` wraps `list.append` in a `try` — `list.append` cannot raise** | `try` block is dead code except on OOM (which is unrecoverable anyway). | Remove the `try/except`. | ☑ |
| 8 | L-08 | `audit_sink.py` | 209–230 | **`_OVERFLOW_COUNTER` global lazy-init is racy under concurrent first calls** | Two threads both pass `if _OVERFLOW_COUNTER is None` simultaneously and both initialise it. | Initialise `_OVERFLOW_COUNTER` at module load time (top-level assignment). | ☑ |
| 9 | L-09 | `translator/injection_filter.py` | Various | **Pattern `\bdan\b.{0,20}(mode\|jailbreak\|prompt)` blocks legitimate prompts mentioning a person named "Dan"** | Over-broad regex produces false positives for common names in professional contexts (e.g. "Dan reviewed the prompt"). | Tighten the pattern or add a whitelist for common false-positive contexts. Document the heuristic's limitations. | ☑ |
| 10 | L-10 | `translator/_json.py` | Various | **Character-by-character JSON walk on potentially large LLM outputs** | `json.JSONDecoder.raw_decode()` would be both faster and more correct. | Replace manual character walk with `json.JSONDecoder().raw_decode(text, idx)`. | ☑ |
| 11 | L-11 | `translator/_sanitise.py` + `translator/injection_filter.py` | Various | **Two independent regex defenders for injection patterns — risk of drift** | Two separate regex pattern sets maintained independently. Patterns can diverge. | Consolidate into a single `_injection_patterns.py` source imported by both modules. | ☑ |
| 12 | L-12 | `translator/llamacpp.py` | `except TimeoutError` | **Dead `except TimeoutError` branch — `create_chat_completion` never raises `TimeoutError`** | The sync `llama_cpp` function raises different error types. `TimeoutError` is never thrown. | Remove the dead branch or replace with the correct exception type from `llama_cpp`. | ☑ |
| 13 | L-13 | `translator/llamacpp.py` | `__init__` | **Model loaded at `__init__` time — blocks main thread for 4+ GB GGUF files; no sharing across Guard instances** | Large model files block startup. Multiple `Guard` instances each load their own copy. | Lazy-load the model on first call. Add a module-level model cache keyed by path so multiple Guards share one loaded model. | ☑ |
| 14 | L-14 | `translator/redundant.py` | 57–78 | **`_raw_strings_agree` is defined but never called anywhere in the codebase** | Dead code. | Remove `_raw_strings_agree` or wire it into the redundancy check where it was presumably intended. | ☑ |
| 15 | L-15 | `translator/redundant.py` | 545–548 | **Unknown `agreement_mode` returns `None` silently** | Should raise `ValueError` with the unsupported mode name. | `raise ValueError(f"Unknown agreement_mode: {agreement_mode!r}")` | ☑ |
| 16 | L-16 | `translator/redundant.py` | line 369 | **Dead `try: import pydantic` guard — Pydantic is a hard dependency** | Pydantic is always installed. The ImportError guard is dead code. | Remove the guard. | ☑ |
| 17 | L-17 | `translator/injection_scorer.py` | 220–244 | **`CalibratedScorer.save`/`load` use `pickle` — loading attacker-controlled `.pkl` is RCE** | The attack surface is not documented. | Add a clear security warning in the docstring. Recommend signing the artifact (e.g. with `hmac`). Consider migrating to `safetensors` or a JSON-based format. | ☑ |
| 18 | L-18 | `_platform.py` | Various | **Two musl detectors with different detection logic produce inconsistent results** | `/lib/ld-musl-*.so.1` glob detection in `_platform.py` disagrees with `ctypes.CDLL("libc.so.6")` check in `cli.py:956`. | Unify musl detection into a single helper in `_platform.py` used by both code paths. | ☑ |
| 19 | L-19 | `__init__.py` | `__all__` | **Multiple types exported but missing from `__all__`** | `RedisDistributedBackend`, `InMemoryDistributedBackend`, `HaystackGuardedComponent`, `PramanixCrewAITool`, `PramanixGuardedModule`, `PramanixSemanticKernelPlugin`, `PramanixPydanticAIValidator`, `StateLoader`, `JWTExpiredError`, `JWTVerificationError`, `RedisStateLoader`, `IdentityClaims`, `StateLoadError`, `fast_path.SemanticFastPath`, `fast_path.FastPathRule` are referenced in docs/code but not in `pramanix.__all__`. | Add all missing symbols to `pramanix.__all__`. Write a test that imports every symbol in `__all__` and asserts it is not `None`. | ☑ |
| 20 | L-20 | Cross-cutting | All translators | **No LLM client connection pooling — TCP+TLS re-established per call** | Every `OpenAICompatTranslator`, `AnthropicTranslator`, `CohereTranslator`, etc. creates a new client object on every `extract()` call. | Latency overhead per call; wasted connections; potential port exhaustion under load. | Construct and store the HTTP client once in `__init__`. Reuse across all calls. Close in `close()` / `__aexit__`. | ☑ |

---

## 5. Packaging & Configuration

| # | ID | Issue | Severity | Fix Action | Done |
|---|-----|-------|----------|------------|------|
| 1 | P-01 | 10 integration packages absent from `[tool.poetry.extras]` — install instructions lead nowhere | Critical | Define all extras in `pyproject.toml`. Run `poetry lock`. | ☑ |
| 2 | P-02 | `anthropic = "^0.25"` incompatible with `httpx >= 0.28.1` — fresh installs broken | High | Bump to `anthropic = "^0.97"`. Run `poetry lock`. | ☑ |
| 3 | P-03 | `ruff target-version = "py312"` vs `python = ">=3.13"` | Medium | Change to `target-version = "py313"`. | ☑ |
| 4 | P-04 | `fail_under = 95` while team targets 98% | Medium | Set `fail_under = 98`. | ☑ |
| 5 | P-05 | `exclude_lines = ["raise NotImplementedError"]` hides untested concrete providers | Medium | Scope exclusion to `# pragma: abstract` only. | ☑ |
| 6 | P-06 | 86 `# pragma: no cover` annotations — 5 cover security/error paths | Medium | Remove the 5 indefensible pragmas and write tests. | ☑ |
| 7 | P-07 | `openai_compat.py` excluded from coverage gate despite being a full implementation with installed deps | Medium | Remove exclusion from `pyproject.toml`. Add tests. | ☑ |
| 8 | P-08 | `setup.cfg` `[flake8]` config dead — project uses ruff | Medium | Remove `[flake8]` section from `setup.cfg`. | ☑ |
| 9 | P-09 | `pyrightconfig.json` `basic` vs mypy `strict` — IDE clean ≠ CI clean | Medium | Set `"typeCheckingMode": "strict"` in `pyrightconfig.json`. | ☑ |
| 10 | P-10 | Ruff missing `S` (security) and `ASYNC` rules | Medium | Add `"S", "ASYNC"` to `[tool.ruff.lint] select`. | ☑ |

---

## 6. CI & DevOps

| # | ID | Issue | Severity | Fix Action | Done |
|---|-----|-------|----------|------------|------|
| 1 | D-01 | K8s init container crashes — `Policy()` and `d.outcome` broken | Critical | Fix warmup script (see C-02). | ☑ |
| 2 | D-02 | K8s image hardcoded `0.8.0`, package at `1.0.0` | High | Introduce Helm/Kustomize variable `{{ .Values.image.tag }}`. | ☑ |
| 3 | D-03 | `pip-audit --skip-editable` never scans pramanix itself | High | Remove `--skip-editable` flag. | ☑ |
| 4 | D-04 | Missing extras never installed in CI — fake `sys.modules` cover all non-OpenAI backends | High | Add CI job with `--extras all` (once extras are defined) running `tests/integration/`. | ☑ |
| 5 | D-05 | CI comment references nonexistent `CVE-2026-1703` | Medium | Remove fabricated CVE reference from comment. | ☑ |
| 6 | D-06 | `Dockerfile.production` installs without hashes — non-reproducible | Medium | Use `pip install --require-hashes -r requirements.txt`. Generate with `pip-compile --generate-hashes`. | ☑ |
| 7 | D-07 | Integration test suite structurally dead in standard CI | Medium | Add CI job installing all extras + running `tests/integration/` with testcontainers. | ☑ |

---

## 7. Test Suite Integrity

| # | ID | Issue | Severity | What It Misses | Fix Action | Done |
|---|-----|-------|----------|----------------|------------|------|
| 1 | T-01 | `_FakeKafkaProducer` replaces `confluent_kafka.Producer` | High | Real delivery callbacks, broker backpressure, threading model | Use testcontainers Kafka for integration tests | ☐ |
| 2 | T-02 | `_FakeS3Client` replaces `boto3.client('s3')` | High | Real AWS auth, network errors, multipart limits | Use `moto` or LocalStack | ☐ |
| 3 | T-03 | `_FakeAsyncpgModule` replaces `asyncpg` | High | Real Postgres pool, `UniqueViolationError`, TLS | Use testcontainers Postgres | ☐ |
| 4 | T-04 | `_FakeLlama` replaces `llama_cpp.Llama` | High | Real GGUF loading, token limits, inference timing | Run a small GGUF model in integration CI | ☐ |
| 5 | T-05 | `_FakeHvacClient` replaces `hvac.Client` | High | Real Vault auth, lease renewal, 403/429 responses | Use testcontainers Vault | ☐ |
| 6 | T-06 | `_FakeSecretsClient` replaces `azure.keyvault.secrets` | High | Real MSAL auth, secret versioning | Use Azure SDK test fixtures / emulator | ☐ |
| 7 | T-07 | `MagicMock()` for Gemini replaces `google.generativeai` | Medium | Real API rate limits, response schema, safety filters | Use `respx` to intercept HTTP + replay real response fixtures | ☐ |
| 8 | T-08 | `MagicMock()` for Cohere replaces `cohere.AsyncClientV2` | Medium | Real token counting, stream handling, retry headers | Same as T-07 | ☐ |

---

## 8. Cross-Cutting Issues

| # | ID | Pattern | Affected Files | Severity | Fix Action | Done |
|---|-----|---------|---------------|----------|------------|------|
| 1 | X-01 | `asyncio.get_event_loop()` (deprecated in 3.12+, raises in future Python) | `circuit_breaker.py:725`, `execution_token.py:893`, `cohere.py:172`, `gemini.py:164`, `llamacpp.py:110` | High | Replace with `asyncio.run()` or `asyncio.get_running_loop()` as appropriate | ☑ |
| 2 | X-02 | `str(exc)` leaks raw exception text into user-facing responses | `crewai.py`, `semantic_kernel.py`, `k8s/webhook.py`, `grpc.py`, `fastapi.py`, `dspy.py` | Medium | Log internally; return generic structured error | ☑ |
| 3 | X-03 | No circuit breaker on translator calls | All translators | Medium | Wrap `extract()` in SDK's own `CircuitBreaker` | ☑ |
| 4 | X-04 | No Prometheus metrics for LLM failures | All translators | Medium | Add `extraction_failure_total`, `consensus_failure_total` counters | ☑ |
| 5 | X-05 | `Decision.__hash__` crashes on unhashable dict fields | `decision.py` | Medium | Override `__hash__` to use only hashable fields | ☑ |
| 6 | X-06 | No `__del__` / cleanup on resource-holding classes | `WorkerPool`, `RedisDistributedBackend`, `PramanixKafkaConsumer` | Medium | Add `__del__` calling `shutdown()` with a deprecation-style warning | ☑ |
| 7 | X-07 | `crypto.py sign()` returns `""` on any error — no metric | `crypto.py` | Medium | Add `pramanix_signing_failure_total` Prometheus counter | ☑ |
| 8 | X-08 | `Decision._compute_hash` uses wrong field for `policy_hash` | `decision.py` | Medium | Use `self.policy_hash` directly | ☑ |
| 9 | X-09 | No LLM client connection pooling — new TCP+TLS per call | All translators | Low | Construct HTTP client once in `__init__`; reuse across calls | ☑ |

---

## 9. Current Coverage Snapshot

> Run as of 2026-04-25 on Windows Python 3.13.7 — `tests/unit/` only.

```
Total:    5580 statements   63 missed   1436 branches   30 partial branches
Coverage: 98.65%   (gate: 95% — recommend raising to 98%)
Result:   2865 passed, 1 skipped, 13 warnings in 134.96s
```

### Files Below 98%

| File | Cover | Missing Lines / Branches |
|------|-------|--------------------------|
| `interceptors/grpc.py` | 94% | 47–49 (streaming RPC paths — see H-15) |
| `translator/injection_filter.py` | 94% | 209 (fail-open exception — see M-36 / L-09) |
| `cli.py` | 95% | 582–583, 657, 766–767, 872–874, 954–971, 994–995, 1012–1013 |
| `interceptors/kafka.py` | 95% | 47–50 (DLQ + commit failure — see M-36) |
| `translator/llamacpp.py` | 96% | 129–130 |
| `audit_sink.py` | 96% | 183, 195–196, 225–226 |
| `crypto.py` | 97% | 365–366 (sign failure path — see M-49) |
| `audit/archiver.py` | 97% | 200, 226–230, 231 |
| `policy.py` | 98% | 238–243 (MRO walk failure — see M-25) |
| `circuit_breaker.py` | 98% | 331–332, 742–743 |